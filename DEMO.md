# NVIS v0.2.0 - 智能可视化演示

## 新功能亮点

### 🎯 智能表格识别

NVIS 现在能够自动识别表格类型并选择最佳可视化方式，无需用户手动配置。

### 📊 四种可视化类型

#### 1. 箱线图 (Box Plot) - CUDA API 性能分析

**最佳用途：** 分析 CUDA Runtime API 调用的性能特征

**示例表格：** `CUPTI_ACTIVITY_KIND_RUNTIME`

**显示效果：**
```
CUPTI_ACTIVITY_KIND_RUNTIME - API Call Statistics
┌─────────────────────────────────────────────────┐
│ ● cudaMemcpy                                    │
│   Count: 6  Mean: 413.83μs  Median: 435.40μs   │
│   330.5 ├────┤████████┼████┤─────────── 480.1  │
│                                                 │
│ ● cudaMemcpyAsync                               │
│   Count: 3  Mean: 203.70μs  Median: 220.60μs   │
│   190.2 ├─────┤████┼███┤──────────────── 220.6 │
│                                                 │
│ ● cudaMalloc                                    │
│   Count: 4  Mean: 147.95μs  Median: 144.05μs   │
│   120.8 ├─────┤██┼██┤────────────────── 180.2  │
└─────────────────────────────────────────────────┘
```

**关键信息：**
- **Count**: 调用次数
- **Mean**: 平均耗时
- **Median**: 中位数（更能代表典型值）
- **箱线图**: 
  - 左端：最小值
  - 左框：Q1 (25%)
  - 中线：中位数
  - 右框：Q3 (75%)
  - 右端：最大值

**如何使用：**
1. 快速识别最耗时的 API 调用
2. 比较不同 API 的性能特征
3. 发现性能异常值（箱线图超长的部分）
4. 评估性能稳定性（箱体宽度）

#### 2. 时间线 (Timeline) - 内核执行可视化

**最佳用途：** 查看内核执行的时序关系

**示例表格：** `CUPTI_ACTIVITY_KIND_KERNEL`, `events`

**显示效果：**
```
CUPTI_ACTIVITY_KIND_KERNEL - Timeline
┌─────────────────────────────────────────────────┐
│ vectorAdd          │███                    │ 500.5μs │
│ matrixMul          │   ████████████        │ 2000.3μs│
│ vectorAdd          │               ███     │ 450.2μs │
│ reduce             │                  ████ │ 800.6μs │
│ vectorAdd          │                    ███│ 480.1μs │
└─────────────────────────────────────────────────┘
```

**关键信息：**
- 每行代表一个内核执行
- 横条位置表示执行时间
- 横条长度表示持续时间
- 可以看到内核间的间隙和重叠

**如何使用：**
1. 识别串行执行模式
2. 发现 GPU 空闲时间
3. 分析内核调度效率
4. 评估并发度

#### 3. 统计文本 (Statistics) - 快速数据概览

**最佳用途：** 查看表格的统计汇总信息

**示例表格：** `statistics`, `device_info`

**显示效果：**
```
statistics - Statistics
┌─────────────────────────────────────────────────┐
│ Table: statistics                               │
│ Columns: 3                                      │
│ Rows: 5                                         │
│                                                 │
│ value:                                          │
│   Mean: 2805.20                                 │
│   Min: 125.50                                   │
│   Max: 7500.50                                  │
└─────────────────────────────────────────────────┘
```

**如何使用：**
1. 快速了解表格结构
2. 查看数值列的范围
3. 获取关键性能指标

#### 4. 柱状图 (Bar Chart) - 通用数据

**最佳用途：** 显示一般数值数据

**显示效果：** 第一个数值列的前10行数据

## 实际使用场景

### 场景 1: 性能瓶颈分析

**问题：** 程序运行慢，不知道哪里有问题

**步骤：**
1. 打开 `CUPTI_ACTIVITY_KIND_RUNTIME` 表
2. 查看箱线图，按平均耗时排序
3. 识别 Mean 值最高的 API
4. 关注 Count 高且 Mean 大的调用
5. 检查箱体宽度大的调用（性能不稳定）

**示例分析：**
```
● cudaMemcpy
  Count: 6  Mean: 413.83μs  Median: 435.40μs
  → 问题：高频调用，平均耗时长
  → 建议：考虑使用 cudaMemcpyAsync 或减少传输次数
```

### 场景 2: GPU 利用率分析

**问题：** GPU 利用率不高

**步骤：**
1. 打开 `CUPTI_ACTIVITY_KIND_KERNEL` 表
2. 查看时间线可视化
3. 寻找内核之间的空白区域
4. 分析是否有并发机会

**示例分析：**
```
vectorAdd   │███         │  ← GPU空闲
matrixMul   │   ████████ │
            ↑ 这里有间隙，可能是数据传输或同步导致
```

### 场景 3: 性能对比

**问题：** 优化前后效果如何

**步骤：**
1. 分别加载优化前后的数据库
2. 对比同一 API 的箱线图
3. 比较 Mean, Median, Max 值
4. 查看调用次数变化

## 表格识别规则

NVIS 使用以下规则自动选择可视化类型：

| 表格特征 | 可视化类型 | 触发条件 |
|----------|-----------|---------|
| 包含 "cuda", "runtime", "api" | BoxPlot | + 有 name 和 duration 列 |
| 包含 "kernel" 或 "event" | Timeline | + 有 start 和 duration 列 |
| 包含 "stat", "summary", "info" | Statistics | 任何表格结构 |
| 其他 | BarChart | 默认类型 |

## 性能提示

### 箱线图解读

1. **箱体宽度**
   - 窄：性能稳定
   - 宽：性能波动大，需要调查

2. **Mean vs Median**
   - 接近：正态分布
   - Mean > Median：有慢速异常值
   - Mean < Median：有快速异常值

3. **须线长度**
   - 短：数据集中
   - 长：有极端值

### 优化优先级

根据箱线图确定优化优先级：

1. **高优先级**
   - Count 高 + Mean 大
   - 箱体宽，性能不稳定
   
2. **中优先级**
   - Count 低 + Mean 很大
   - 偶尔出现的慢调用

3. **低优先级**
   - Count 低 + Mean 小
   - 对整体性能影响小

## 快捷键

- `↑/↓`: 滚动数据
- `←/→`: 切换面板
- `Tab`: 循环切换焦点
- `Enter`: 加载选中的表格
- `q`: 退出

## 数据要求

为了获得最佳可视化效果：

1. **API调用表**
   - 必须有：function name, duration
   - 建议有：start, end, correlationId

2. **内核表**
   - 必须有：name, start, duration
   - 建议有：end, gridX, blockX

3. **统计表**
   - 至少一个数值列
   - 建议有：metric name, value

## 示例数据库

运行以下命令创建示例数据库：

```bash
./create_sample_db.sh
```

这将创建包含以下表格的数据库：
- `CUPTI_ACTIVITY_KIND_RUNTIME` - API 调用数据
- `CUPTI_ACTIVITY_KIND_KERNEL` - 内核执行数据
- `events` - 事件序列数据
- `statistics` - 统计汇总数据
- `device_info` - 设备信息

## 未来功能

计划添加的功能：
- [ ] 散点图 - 用于相关性分析
- [ ] 热力图 - 用于矩阵数据
- [ ] 直方图 - 用于分布分析
- [ ] 自定义可视化规则
- [ ] 数据过滤和搜索
- [ ] 导出可视化图表

## 技术细节

- **统计计算**: 使用四分位数算法
- **数据聚合**: 按函数名分组
- **渲染优化**: 限制显示项目数量
- **自动适应**: 根据终端大小调整布局
