# 分割面板和可视化功能说明

## 概述

右侧展示区域现在支持两个独立的面板:
1. **表格面板** - 显示数据库表的原始数据
2. **可视化面板** - 以图形方式展示数据

两个面板可以独立开启/关闭,并支持动态调整大小。

## 功能特性

### 1. 面板控制

当你选择一个数据表后,顶部会显示控制按钮:

```
表: table_name  |  ✓ 表格  |  ✓ 可视化  |  返回时间线
```

- **✓ 表格** - 点击切换表格面板的显示/隐藏
- **✓ 可视化** - 点击切换可视化面板的显示/隐藏
- **返回时间线** - 返回到时间线视图

### 2. 显示模式

#### 模式一: 双面板模式 (默认)
- 表格和可视化同时显示
- 使用可调整大小的分割布局
- 表格在上方,可视化在下方
- 拖动分割线可调整两个面板的高度比例
- 点击 **🔄 重置视图** 按钮恢复 50:50 比例

#### 模式二: 仅表格模式
- 关闭可视化面板,表格占据全部空间
- 适合查看详细的表格数据

#### 模式三: 仅可视化模式
- 关闭表格面板,可视化占据全部空间
- 适合专注于数据分析和可视化

## 可视化功能

### 支持的可视化类型

可视化组件会自动检测数据类型并选择合适的展示方式:

#### 1. 散点图 (Scatter Plot)
- **触发条件**: 表中有 2 个或更多数值列
- **显示内容**: 
  - X 轴: 第一个数值列
  - Y 轴: 第二个数值列
  - 每个数据点显示为蓝色圆点
  - 顶部显示数据范围和点数统计

**示例场景**:
```
列: id (数值), timestamp (数值), value (数值)
→ 绘制 id vs timestamp 的散点图
```

#### 2. 柱状图 (Bar Chart)
- **触发条件**: 表中有 1 个数值列
- **显示内容**:
  - 每行数据对应一个柱子
  - 柱子高度反映数值大小
  - 自动缩放以适应数据范围

**示例场景**:
```
列: name (文本), count (数值)
→ 绘制 count 的柱状图
```

#### 3. 数据摘要
- **触发条件**: 表中没有数值列
- **显示内容**:
  - 列数
  - 行数
  - 提示信息

### 交互操作

#### 缩放 (Zoom)
- **鼠标滚轮**: 向上滚动放大,向下滚动缩小
- **缩放范围**: 10% - 1000%
- **缩放中心**: 鼠标位置
- **当前缩放**: 显示在右下角 (例: "缩放: 150%")

#### 平移 (Pan)
- **鼠标拖拽**: 按住鼠标左键拖动视图
- **适用场景**: 放大后查看不同区域

#### 重置视图
- **按钮位置**: 可视化面板顶部 "🔄 重置缩放"
- **效果**: 
  - 缩放级别恢复到 100%
  - 偏移量归零
  - 视图回到初始状态

### 网格背景

- 可视化区域显示网格背景,帮助对齐和估计距离
- 网格间距随缩放级别动态调整
- 网格颜色: 浅灰色,不干扰数据展示

## 使用场景

### 场景 1: 探索性数据分析
```
1. 选择包含数值数据的表
2. 启用双面板模式
3. 在表格中查看原始数据
4. 在可视化中观察数据分布和趋势
5. 使用滚轮放大感兴趣的区域
```

### 场景 2: 详细数据检查
```
1. 选择数据表
2. 关闭可视化面板
3. 表格全屏显示,方便查看细节
4. 使用分页浏览大量数据
```

### 场景 3: 数据可视化分析
```
1. 选择数据表
2. 关闭表格面板
3. 可视化全屏显示
4. 使用缩放功能查看细节
5. 拖动查看不同区域
```

### 场景 4: CUDA 性能分析
```
1. 加载 NSYS 导出的 SQLite 文件
2. 选择 CUPTI_ACTIVITY_KIND_KERNEL 表
3. 查看表格数据中的 kernel 信息
4. 在可视化中观察 start/end 时间分布
5. 缩放查看特定时间段的 kernel 调用
```

## 技术细节

### 数值列检测
- 对每列的前 10 行进行采样
- 尝试解析为 f64 浮点数
- 如果 >70% 的样本可解析,认为是数值列
- 自动选择合适的可视化类型

### 坐标转换
```
数据坐标 → 屏幕坐标
  ↓
应用缩放和偏移
  ↓
最终显示位置
```

### 性能优化
- 只绘制可见区域的数据
- 网格使用简单的线段绘制
- 避免不必要的重绘

## 键盘快捷键 (未来计划)

以下功能计划在未来版本中添加:

- `Space` - 切换表格/可视化面板
- `R` - 重置视图
- `+` / `-` - 缩放
- `←` `→` `↑` `↓` - 平移视图

## 示例数据

### 适合散点图的数据
```sql
CREATE TABLE performance (
    id INTEGER,
    time REAL,
    value REAL
);
```

### 适合柱状图的数据
```sql
CREATE TABLE statistics (
    name TEXT,
    count INTEGER
);
```

## 故障排除

### 问题: 没有显示可视化
**原因**: 数据中没有数值列
**解决**: 确保表中至少有一列是数值类型

### 问题: 散点图点重叠
**原因**: 数据点密集
**解决**: 使用滚轮放大,查看局部区域

### 问题: 柱状图看不清
**原因**: 数据行数太多
**解决**: 
1. 使用分页只加载部分数据
2. 放大查看特定区域

### 问题: 面板比例不合适
**解决**: 
1. 拖动分割线调整
2. 点击 "🔄 重置视图" 恢复默认比例

## 日志支持

所有关键操作都有日志记录:

```
[INFO] 用户选择了数据表: my_table
[DEBUG] 切换表格面板: true
[DEBUG] 切换可视化面板: true
[DEBUG] 缩放级别变化: 1.00 -> 1.50
[DEBUG] 拖拽结束，当前偏移: Vec2 { x: 100.0, y: 50.0 }
[INFO] 重置可视化视图
```

设置日志等级为 DEBUG 或 TRACE 可以查看详细的交互信息。

## 扩展性

可视化组件设计为可扩展的,未来可以添加:

- 折线图 (Line Chart)
- 饼图 (Pie Chart)  
- 直方图 (Histogram)
- 热力图 (Heatmap)
- 3D 可视化
- 自定义图表类型

每种可视化类型都可以独立实现,并在检测到合适的数据时自动选择。
