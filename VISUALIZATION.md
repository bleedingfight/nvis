# 智能可视化系统说明

NVIS v0.2.0 引入了智能可视化系统，能够根据表格类型和内容自动选择最合适的可视化方式。

## 支持的可视化类型

### 1. 箱线图 (Box Plot) - CUDA API调用统计

**适用场景:**
- 表格名包含 "cuda", "runtime", "api" 或 "CUPTI_ACTIVITY_KIND_RUNTIME"
- 包含函数名和duration/time列

**显示内容:**
- 每个API函数的调用统计
- 最小值 (Min)
- 第一四分位数 (Q1)
- 中位数 (Median)
- 第三四分位数 (Q3)
- 最大值 (Max)
- 平均值 (Mean)
- 调用次数 (Count)

**可视化示例:**
```
● cudaMemcpy
  Count: 6  Mean: 413.83μs  Median: 435.40μs
  330.5 ├────┤████████████┼████████┤─── 480.1
```

**特点:**
- 按平均耗时降序排列
- 显示前10个最耗时的函数
- 清晰展示性能分布和异常值

### 2. 时间线 (Timeline) - 内核执行序列

**适用场景:**
- 表格名包含 "kernel" 或 "event"
- 包含 name, start, duration 列

**显示内容:**
- 事件/内核的执行顺序
- 相对时间位置
- 持续时间

**可视化示例:**
```
vectorAdd          │████              │ 500.5μs
matrixMul          │    ████████████  │ 2000.3μs
reduce             │        ████       │ 800.6μs
```

**特点:**
- 直观展示执行时序
- 可以看到并发和间隙
- 显示最多20个事件

### 3. 统计文本 (Statistics) - 数值汇总

**适用场景:**
- 表格名包含 "stat", "summary", "info"
- 或无法识别为其他类型的表格

**显示内容:**
- 表格基本信息
- 每个数值列的统计信息:
  - 平均值 (Mean)
  - 最小值 (Min)
  - 最大值 (Max)

**示例:**
```
Table: statistics
Columns: 3
Rows: 5

value:
  Mean: 2805.20
  Min: 125.50
  Max: 7500.50
```

### 4. 柱状图 (Bar Chart) - 通用数据

**适用场景:**
- 默认可视化类型
- 用于不符合上述模式的表格

**显示内容:**
- 第一个数值列的数据
- 显示10行数据

## 智能检测算法

系统通过以下方式检测表格类型:

1. **表格名称检测**
   - 检查表格名是否包含特定关键词
   - 例如: "cuda", "kernel", "runtime", "api"

2. **列名检测**
   - 分析列名以识别数据特征
   - 例如: "start", "end", "duration", "name"

3. **数据内容分析**
   - 检查是否有足够的数值数据
   - 验证数据是否适合特定可视化

## 使用示例

### CUDA API性能分析

1. 打开包含 CUDA Runtime API 数据的表格
2. 系统自动识别为 API 调用数据
3. 显示箱线图统计:
   - 快速识别性能瓶颈
   - 查看调用耗时分布
   - 发现异常值

**最佳实践:**
- 关注 Mean 和 Median 差异大的函数 (可能有性能问题)
- 查看 Max 值特别大的函数 (可能的性能瓶颈)
- Count 高且 Mean 大的函数是优化重点

### 内核执行分析

1. 打开包含内核执行记录的表格
2. 系统显示时间线可视化
3. 分析执行模式:
   - 识别串行执行的内核
   - 发现执行间隙
   - 评估负载均衡

### 统计信息查看

1. 打开统计汇总表格
2. 快速浏览关键指标
3. 理解整体性能特征

## 可视化配置

当前版本的可视化参数:

| 参数 | 值 | 说明 |
|------|---|------|
| BoxPlot最大显示 | 10 | 显示前10个最耗时的函数 |
| Timeline最大显示 | 20 | 显示最多20个事件 |
| BarChart最大显示 | 10 | 显示10个数据点 |
| 数据加载限制 | 1000行 | 每个表格最多加载1000行 |

## 性能优化建议

### CUDA API优化
通过箱线图可以快速识别:
1. **高频高耗时调用** - 优先优化目标
2. **耗时波动大的调用** - 可能存在性能不稳定
3. **异常慢的调用** - 需要深入分析

### 内核优化
通过时间线可以发现:
1. **执行间隙** - GPU利用率不足
2. **串行执行** - 缺少并发
3. **不均衡负载** - 某些内核特别慢

## 自定义和扩展

未来版本将支持:
- 用户自定义可视化规则
- 更多图表类型 (散点图、热力图等)
- 交互式图表操作
- 导出可视化结果

## 故障排除

### 可视化不显示
- 检查表格是否有数据
- 确认列名是否正确
- 某些表格可能需要特定的列结构

### 数据显示不完整
- 默认只显示前10/20个项目
- 可以滚动查看更多数据
- 考虑在数据库层面过滤数据

### 性能统计不准确
- 确保 duration 列的单位一致
- 检查是否有 NULL 或异常值
- 验证数值列的数据类型

## 技术实现

可视化系统位于 `src/visualization.rs`:
- `detect_table_type()` - 表格类型检测
- `generate_boxplot_data()` - 箱线图数据生成
- `generate_timeline_data()` - 时间线数据生成
- `generate_statistics_text()` - 统计文本生成
- `generate_visualization()` - 主入口函数
